name: format

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - master

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: j178/prek-action@v3.0.0
